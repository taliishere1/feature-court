import { Great_Vibes } from "next/font/google";

/** Great Vibes — signature only. Loaded via next/font so it always renders. */
export const judgeSignatureFont = Great_Vibes({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
  variable: "--font-judge-signature",
});
